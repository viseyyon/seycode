# js

To install dependencies:

```bash
sey install
```

To run:

```bash
bun run index.ts
```

This project was created using `sey init` in bun v1.2.12. [Bun](https://bun.sh) is a fast all-in-one JavaScript runtime.
